services:
  igdb_proxy.commands:
    class: Drupal\igdb_proxy\Commands\IgdbProxyCommands
    arguments: ['@queue', '@plugin.manager.queue_worker', '@file_system', '@database', '@state', '@datetime.time']
    tags:
      - { name: drush.command }
